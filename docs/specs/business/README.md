# specs / business — 业务规格

## 作用

存放 **业务侧** 的详细设计与规格：用户旅程、业务规则、验收口径、数据在业务语义下的含义等（不绑定具体表结构或 API 字段名）。

## 约定

- 与 `requirements/` 对齐；技术实现细节放在 `specs/technical/`。
- 需要追溯时在 `traceability/` 建立需求 ID 与文档的对应关系。

## 当前文档

（暂无；随需求基线重建后补充。）
